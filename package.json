{
  "name": "thefood-court",
  "private": true,
  "version": "1.0.0",
  "description": "Pre-order pickup app for The Food Court, EASTC campus.",
  "type": "module",
  "scripts": {
    "start": "npx --yes serve -s . -l 5173",
    "test": "node --test tests/*.test.js",
    "api:dev": "npx --yes wrangler@4 dev",
    "api:deploy": "npx --yes wrangler@4 deploy"
  },
  "engines": {
    "node": ">=18"
  }
}
